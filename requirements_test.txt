-r requirements.txt
pytest-homeassistant-custom-component==0.13.340
aioresponses==0.7.8
# Pinned by pytest-homeassistant-custom-component==0.13.340 (requires syrupy==5.1.0);
# can't bump independently until that package relaxes the pin.
syrupy==5.1.0

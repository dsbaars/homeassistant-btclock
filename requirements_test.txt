-r requirements.txt
pytest-homeassistant-custom-component==0.13.333
aioresponses==0.7.9
# Pinned by pytest-homeassistant-custom-component==0.13.333 (requires syrupy==5.5.3);
# can't bump independently until that package relaxes the pin.
syrupy==5.1.0

name: "Test"

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  pytest:
    name: "Pytest"
    runs-on: "ubuntu-latest"
    # See .forgejo/workflows/lint.yml — actions/setup-python is unreliable on
    # Forgejo's runner, so pull Python from its official image instead.
    container:
      image: "python:3.14-slim"
    steps:
      - name: "Checkout the repository"
        uses: "actions/checkout@v4"

      - name: "Install requirements"
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements_test.txt

      - name: "Run pytest"
        run: python -m pytest tests/ -v
